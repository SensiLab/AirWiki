remote_theme: pages-themes/minimal@v0.2.0
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one

baseurl: "/AirWiki" # Replace with your repository name if different

title: [AirWiki]
description: [A knowledge hub for all things AirSticks]

url: "https://sensilab.github.io" # Note: Do not include the trailing slash
